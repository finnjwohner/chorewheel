import 'package:flutter/material.dart';

class ChorePage extends StatelessWidget {
  const ChorePage({super.key});

  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Column(
        children: [
          
        ],
      ),
    );
  }
}